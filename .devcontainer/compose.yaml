services:
  devcontainer:
    build: ./.devcontainer
    command: sleep infinity
    volumes:
      - type: bind
        source: .
        target: /workspaces/fumimi-channel
        consistency: cached
    init: true